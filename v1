#include<bits/stdc++.h>
using namespace std;
int marks_aquired(String *usn , String *name , int n ,int *internal)
{	
	double total=0.0;		
	for(int i=0;i<n;i++)
		{
			total+=internal[i];
		}
	sort(internal,internal+n);
	return (internal[1]+internal[2])/2;
}
